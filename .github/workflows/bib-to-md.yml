name: (disabled) Generate markdown from BibTeX

# This workflow is no longer needed.
# jekyll-scholar reads _bibliography/*.bib directly during the Jekyll build.
# Just update _bibliography/publications.bib or _bibliography/talks.bib and push.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is disabled. Use jekyll-scholar instead."
        required: false
